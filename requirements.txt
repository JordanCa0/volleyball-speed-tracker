opencv-python>=4.9.0
numpy>=1.26.0
ultralytics>=8.2.0

# v2 tracking pipeline
supervision>=0.30.0,<0.31.0   # InferenceSlicer, Detections, annotators
trackers>=2.0.0               # ByteTrackTracker (sv.ByteTrack is removed in 0.31)
matplotlib>=3.8.0             # ColorPalette.from_matplotlib, for the ball trail

# web UI
flask>=3.0.0                  # upload page + job polling (webapp.py)
